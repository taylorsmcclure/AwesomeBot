const domain = require("domain");

// Run an extension (command, keyword, or timer)
module.exports = (bot, winston, svr, ch, extensionDocument, msg, suffix, keywordMatch) => {
	// TODO: actually do something 
};